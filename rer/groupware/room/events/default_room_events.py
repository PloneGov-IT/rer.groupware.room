# -*- coding: utf-8 -*-


def createAreas(room, event):
    """
    This event throw some specified events for room initial setup
    """


def createGroups(room, event):
    """
    This event throw some specified events for room initial setup
    """


def createRules(room, event):
    """
    This event throw some specified events for room initial setup
    """


def createHomePage(room, event):
    """
    This event throw some specified events for room initial setup
    """


def createSharing(room, event):
    """
    This event throw some specified events for room initial setup
    """